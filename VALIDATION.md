# Pulse Windows 1.0.1 验证记录

日期：2026-09-17。Windows x64；本机桌面 2560 × 1440、100% 缩放。

## 已验证

- TypeScript 类型检查、Vite 生产构建通过。
- 19 项 Node 测试通过：缺失额度与零值区分、Codex 多窗口与耗尽标记、Cursor 百分比单位、GLM 数值优先级、Kimi 字符串计数、MiniMax 剩余值换算、Copilot 无限额度、DeepSeek 余额、Grok / Grok Bot、Ollama HTML、Antigravity / Volcengine / Devin / Command Code 上游样本、历史去重和禁止明文凭据回退。
- Electron 桌面检查通过：设置页、置顶且不抢焦点、悬停卡片、设置持久化、额度固定显示、历史无记录状态、关于页、更新界面、外部链接白名单、账号增删、中英文切换、顶部停靠、自动收起、无渲染异常。
- Windows 原生鼠标检查通过：实际鼠标拖动可从贴边切换为自由悬浮；透明空白区域的点击能够传递到后方专用测试窗口。
- 透明窗口截图的空白像素 alpha 为 0。
- Windows `safeStorage` 实际保存合成测试凭据后，磁盘文件不包含明文；测试结束清除该合成凭据。
- 全屏检测辅助进程可以正常启动、输出有效状态，并受父进程生命周期约束。未宣称所有全屏应用均已验证。
- npm 官方仓库生产依赖审计：0 vulnerabilities。默认镜像不提供审计 API，因此改用 npm 官方审计接口。
- 发布后以隔离数据目录运行打包程序，从公开 GitHub Releases 在线检查更新，正确返回“当前已是最新版本”；`latest.yml` 与 4 个 Release 资产均可公开读取。
- 主要源码、样式和使用说明没有 Unicode 替换字符。

## 真实账号查询

| 服务 | 本机结果 |
|---|---|
| Codex | 使用现有本地登录，返回 live 状态及 3 个额度窗口 |
| Claude Code | 未找到可用本地登录，正确显示 unavailable；没有使用虚构数据 |
| 其余服务 | 尚未完成逐一真实账号验证；部分使用上游响应样本验证了解析 |

检查过程没有打印凭据、账户身份或具体额度值。模拟界面截图使用隔离的合成数据，不能作为真实账号接入证据。

## 界面对照

设计依据为上游 `Docs/panel.webp`、`Docs/settings.webp` 和 SwiftUI 源码。使用 frontend-app-builder 的对照流程，并以既有原版设计为准。
会话没有可用 Browser/IAB 工具，因此使用 Playwright Electron 检查真实桌面，再使用 Edge Chromium 在中性背景下截图。已用图片查看工具对照原版及实现截图。

| 对照点 | 实现及差异 |
|---|---|
| 悬浮条 | 64 逻辑像素宽；自由模式胶囊，停靠模式采用原版凹曲线与指数 4 连续圆角路径 |
| 圆环 | 36 逻辑像素直径、4 像素描边、原版绿色 / 警告色、复用服务 SVG |
| 卡片 | 250 逻辑像素宽、20 像素圆角、6 像素进度条、连接尖角；增加可用的刷新与账号设置入口 |
| 设置页 | 侧边栏、搜索、分组行、蓝色开关和分段选择器沿用原版结构；增加 Windows 窗口操作按钮 |
| 字体和排版 | 使用 Windows 字体；字形、文字栅格化、部分间距与 macOS 不完全相同 |
| 文案 | 通用设置保留原功能顺序；玻璃选项明确为 Windows 模拟。默认中文，支持英文 |
| 材质与动效 | 黑色模式可对照；玻璃为半透明模拟，非 Apple Liquid Glass。收起/展开动效为 Windows 实现 |

截图尺寸：Electron 设置页 1040 × 790；Edge 设置页 1040 × 790 逻辑像素、1.5 倍输出；悬浮窗随账号数量变化。未宣称与上游原始截图同分辨率逐像素验收。
本次实现通过了结构、尺寸、图标、颜色、控件、核心交互的人工对照；上述差异保留并公开记录，**不能标记为全 UI 100% 一致**。

## 边界

- Windows x64 安装包；没有 ARM64 构建。
- 未完成混合 DPI 多屏热插拔、长时间后台运行、全部服务登录过期/续期和全部真实套餐形态测试。
- 历史统计当前覆盖 Codex 与 Claude Code，未覆盖原版全部 54 类客户端和全部花费图表。
- 其他功能差异见 README。更新检查以 Windows 版 GitHub Releases 元数据为准；首次发布只能验证最新版本状态，需后续更高版本才能端到端验证升级安装。

## 最终安装包验证

- `npm run dist` 最终退出码 0；关闭发布配置后，更新元数据错误已修复。
- EXE 安装到工作区隔离目录，安装进程退出码 0，`Pulse.exe` 成功落盘。
- 以实际安装目录中的 `Pulse.exe` 重跑桌面交互检查及 Windows 加密凭据检查，全部通过。
- 从测试安装目录执行标准卸载，退出码 0，已确认测试安装的 `Pulse.exe` 被移除。不会要求用户使用该测试安装目录。
- 最终 ASAR 共 5124 个文件；包含 NOTICE，未包含测试目录、QA 输出、上游仓库或 auth / credentials / cache JSON 数据文件。
- 上游仓库 `git status --short` 为空，原版源码未修改。
- 安装包：`Pulse-Windows-1.0.1-Setup.exe`，98,333,755 字节，Authenticode 状态 `NotSigned`。
- SHA-256：`4adc3bd3334a04407a832ee526ce4aff57819d0eb77af0a5684dc6390f591680`。
